---
title: Projects & builds
permalink: /projects/
---
<div class="wrap">
  <div class="page-head">
    <h1>Projects &amp; builds</h1>
    <p>Physical devices, dashboards and models that turn urban and environmental data into something you can look at. Code is open where it can be.</p>
  </div>

  <div class="chips" style="padding:28px 0 48px">
    <span class="chip chip--on">All</span>
    <a class="chip" href="/tag/iot-2/">Data devices</a>
    <a class="chip" href="/tag/dashboard/">Dashboards</a>
    <a class="chip" href="/tag/3d-printing/">3D printed</a>
    <a class="chip" href="/tag/ios-app/">Apps</a>
    <a class="chip" href="/tag/3d-modelling-2/">City models</a>
    <a class="chip" href="/tag/music/">Sound</a>
  </div>

  <div class="tiles">
    {% for r in repos %}
    <a class="tile" href="{{ r.url }}">
      <div class="tile__media"></div>
      <div class="tile__body">
        <div class="mono mono--accent" style="font-size:10px;letter-spacing:0.12em">{{ r.kind }}</div>
        <h3>{{ r.name }}</h3>
        <p>{{ r.blurb }}</p>
        <div class="tile__foot">{{ r.foot }}</div>
      </div>
    </a>
    {% endfor %}
  </div>
  <div style="height:80px"></div>
</div>
